# Full Auth Flask Backend App

This project is a Flaskapplication. It implements user authentication using JSON Web Tokens (JWT), allowing users to create an account safely, log in, and create and manage their own tasks.

Each user has their own collection of tasks, and all task routes are protected so users can only access, update, and delete their own data.



testing used with POSTMAN for the CRUD Operations etc.
